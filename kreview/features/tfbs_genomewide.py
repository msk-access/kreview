# AUTOGENERATED! DO NOT EDIT! File to edit: ../../nbs/features/18b_tfbs_genomewide.ipynb.

# %% ../../nbs/features/18b_tfbs_genomewide.ipynb #6265be8c
from __future__ import annotations
import pandas as pd
import structlog
from ..eval_engine import FeatureEvaluator

log = structlog.get_logger()

# %% auto #0
__all__ = ['log', 'TFBSEvaluator']

# %% ../../nbs/features/18b_tfbs_genomewide.ipynb #0e39738d
class TFBSEvaluator(FeatureEvaluator):
    """Extracts TFBS footprint metrics."""

    name = "TfbsGenomewide"
    source_file = ".TFBS.parquet"
    tier = 2
    category = "epigenetics_and_geometry"

    def extract(self, df: pd.DataFrame) -> dict[str, float]:
        extracted = {}
        try:
            if df.empty:
                return extracted
            cols = set(df.columns)

            if "label" in cols:
                metrics = [
                    c for c in ["count", "mean_size", "entropy", "z_score"] if c in cols
                ]
                for row in df.to_dict("records"):
                    lbl = str(row["label"]).replace(" ", "_").replace("-", "_")
                    for m in metrics:
                        if pd.notna(row[m]):
                            extracted[f"{lbl}_{m}"] = float(row[m])

            return extracted
        except Exception as e:
            log.exception("extraction_failed", evaluator=self.name, error=str(e))
            return {}
